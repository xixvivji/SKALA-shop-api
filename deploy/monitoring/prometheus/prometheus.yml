global:
  scrape_interval: 30s
  scrape_timeout: 10s
  evaluation_interval: 30s

scrape_configs:
  - job_name: skala-shop-api
    metrics_path: /actuator/prometheus
    scheme: http
    static_configs:
      - targets:
          - backend:9090
        labels:
          environment: production
